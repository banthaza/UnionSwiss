import { PercentIntPipe } from './percent-int.pipe';

describe('PercentIntPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentIntPipe();
    expect(pipe).toBeTruthy();
  });
});
